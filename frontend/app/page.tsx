import React, { FC } from "react";
// Components
import { HeroHome } from "../components/Hero";

const Home: FC = (): JSX.Element => {
	return (
		<>
			<HeroHome />
			This is homepage
		</>
	);
};

export default Home;
